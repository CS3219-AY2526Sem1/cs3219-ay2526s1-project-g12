[project]
name = "matching-svc"
version = "0.1.0"
description = "Question Service for PeerPrep"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "asyncio>=4.0.0",
    "fastapi[standard]>=0.118.0",
    "redis>=6.4.0",
    "uuid>=1.30",
]
